import { SERVER_ENDPOINT } from '@/constants'

const config = {
  apiURL: `${SERVER_ENDPOINT}/api/`,
  avatarURL: `${SERVER_ENDPOINT}/storage`,
  shareURL: `${SERVER_ENDPOINT}/event/`,
}

export default config
